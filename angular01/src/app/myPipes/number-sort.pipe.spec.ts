import { NumberSortPipe } from './number-sort.pipe';

describe('NumberSortPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSortPipe();
    expect(pipe).toBeTruthy();
  });
});
