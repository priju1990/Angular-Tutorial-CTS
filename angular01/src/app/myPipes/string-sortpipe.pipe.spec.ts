import { StringSortpipePipe } from './string-sortpipe.pipe';

describe('StringSortpipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringSortpipePipe();
    expect(pipe).toBeTruthy();
  });
});
